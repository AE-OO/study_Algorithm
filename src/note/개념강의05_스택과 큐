개념강의 05. 스택과 큐

◆ 스택
- LIFO 형태, 가장 나중에 들어온 것이 가장 먼저 나감 (삽입 삭제가 한 곳에서 이루어짐)
  스택에 새 값이 들어가면 top은 새 값을 가리키고, 값을 빼낼 때 pop이 top이 가리키는 값을 스택에서 빼냄
      push => top에 새 값을 넣는 연산
      pop => top이 가리키는 값을 꺼냄
      peek => top이 가리키는 값을 단순히 확인만 함

=> 깊이 우선 탐색, 백트래킹 유형의 테스트에서 많이 사용됨
+ 재귀 함수 알고리즘


◆ 큐
- FIFO 형태, 가장 먼저 들어간 값이 가장 먼저 나감 (삽입 삭제가 양방향에서 이루어짐)
      rear => 큐의 가장 끝 데이터
      front => 큐의 가장 앞 데이터
      add => rear에 새로운 데이터를 삽입함
      poll => front에 데이터를 삭제하고 확인함
      peek => front의 값을 단순히 확인함

=> 너비 우선 탐색에서 자주 사용됨


◆ 우선순위 큐 => 값이 들어간 순서와 상관없이 우선순위가 높은 값이 먼저 나오는 자료구조
